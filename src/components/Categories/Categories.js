import {useStaticQuery} from 'gatsby'
import {map} from 'lodash'
import React, {useState} from 'react'
const Categories = ({node}) => {
  return <>{data2.map(data => data)}</>
}

export default Categories
